# 3주차 코드 리뷰 기록

이름 / 학번:

## 1. 실행 전 예측과 실제 결과

실행하기 전에 예상 열을 먼저 작성한다.

| 코드 | 예상 | 실제 | 차이가 난 이유와 수정 방향 |
|---|---|---|---|
| bugs A: 문자열 + 숫자 | | | |
| bugs B: 속성 오타 | | | |
| bugs C: 없는 상품 접근 | | | |
| bugs D: 참조 공유 | | | |

## 2. 코드의 실행 흐름

노트 2개, 회원 할인으로 주문할 때 입력의 값/타입과 호출 순서를 적는다. 파일·함수 이름을 근거로 사용한다.

- 이벤트 등록과 콜백 실행은 각각 언제 일어나는가?
- 문자열은 어디서 숫자가 되는가?
- `find()` 결과와 `Result<number>`는 어떤 조건에서 좁혀지는가?
- `policy(subtotal)`은 어떤 함수를 실행하고 무엇을 반환하는가?
- `await saveOrder()` 전후에 화면은 어떤 상태인가?
- `finally`와 `busy`를 제거하면 어떤 문제가 생길 수 있는가?

## 3. 검증 기록

| 입력/시나리오 | 예상 | 실제 | 근거 또는 수정 |
|---|---|---|---|
| 노트 2개 / 일반 | 6,000원 | | |
| 노트 2개 / 회원 | 5,400원 | | |
| 빈 값 / 공백 | 실패 | | |
| 0 / -1 / 1.5 / abc / 2abc / Infinity | 모두 실패 | | |
| 노트 5개 / 6개 | 성공 / 실패 | | |
| 필통 1개 / 없는 상품 ID (자동 검증) | 실패 / 실패 | | |
| 주문 요청 실패 → 체크 해제 → 재시도 | 오류 → 성공 | | |
| 상품 조회 실패 → 체크 해제 → 다시 불러오기 | 오류 → 복구 | | |
| 주문 버튼 빠르게 두 번 누르기 | 한 번만 처리 | | |
| npm run check / npm test | 통과 / 통과 | | |

## 4. 타입 검사와 실행 검증

- 타입 실험의 오류 메시지 하나와 수정 전후 코드:
- 타입 검사로 찾은 오류와 실행 시 검증해야 하는 오류 각 하나:
- `as Product`가 JSON 검증을 대신할 수 없는 이유:
- `const` 상품 배열을 사용해도 상품 속성이 바뀔 수 있는 이유:
- 순수 함수 안의 `let`·`for...of`가 반드시 부수 효과는 아닌 이유:
- Strategy를 사용했을 때 새 정책을 추가하는 위치:

## 5. 선택 과제와 AI 활용

- Adapter 선택 과제 수행 여부 / fixture별 결과:
- AI에 보낸 질문 / 제안 중 채택·거절한 부분 / 반례 검증:
- 동료에게 설명하고 받은 피드백 / 수정 사항:
