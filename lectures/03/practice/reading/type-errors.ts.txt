// src/type-experiment.ts에 복사한 후 npm run check로 오류를 읽으세요.
// 아래 세 오류를 수정하고 다시 검사하세요. 실험이 끝나면 이 임시 파일을 제거해도 됩니다.
import type { Product } from "./types.js";
import { calculateLineTotal } from "./order.js";
const product: Product = { id: "note", name: "노트", price: 3000, stock: 5 };
calculateLineTotal(product.price, "2");
console.log(product.prcie);
const found = [product].find((item) => item.id === "missing");
console.log(found.price);
// any, @ts-ignore, 무조건적인 as나 !로 검사를 우회하지 마세요.
// 마지막 사례는 if로 존재 여부를 확인한 뒤 접근하세요.
