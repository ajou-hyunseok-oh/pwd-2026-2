{
  "name": "pwd-week3-order-review",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "start": "npm run build && node server.mjs",
    "watch": "tsc --watch",
    "test": "npm run build && node tests/order.test.mjs",
    "test:solution": "npm run build && node tests/order.test.mjs --solution",
    "bugs": "node reading/bugs.js"
  },
  "devDependencies": { "typescript": "5.9.3" }
}
